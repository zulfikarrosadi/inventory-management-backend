meta {
  name: Login
  type: http
  seq: 2
}

post {
  url: {{base_url}}/login
  body: json
  auth: inherit
}

body:json {
  {
      "username": "{{current_username}}",
      "password": "{{password}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
